local diffview = {
    'sindrets/diffview.nvim',
    opts = {
        file_panel = {
            win_config = {
                position = 'right',
            },
        },
    },
}

return { diffview }
